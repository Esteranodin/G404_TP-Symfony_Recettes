{% extends 'base.html.twig' %}

{% block title %}Connexion
{% endblock %}

{% block body %}
	<form method="post">
		{% if error %}
			<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
		{% endif %}

		{% if app.user %}
			<div class="mb-3">
				Vous êtes déjà connecté entant que
				{{ app.user.pseudo }}, si vous voulez vous deconnecter cliquer sur
				<a href="{{ path('app_logout') }}">Deconnexion</a>
			</div>
		{% endif %}

		<section class="form">
			<h2 class="h3 mb-3 font-weight-normal">Connectez-vous</h2>

			<div class="form-group">
				<p class="form-label">
					<label for="username">Votre email de connexion </label>
					<input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="email" required autofocus>
                    </p>
                <p class="form-label">
					<label for="password">Votre mot de passe </label>
					<input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>
					<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
				</p>

				{#
Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
 See https://symfony.com/doc/current/security/remember_me.html
		<div class="checkbox mb-3">
		<input type="checkbox" name="_remember_me" id="_remember_me">
		<label for="_remember_me">Remember me</label></div> #}


				<div class="form-actions">
					<button type="submit" class="btn-submit">
						Connexion
					</button>
				</div>
			</div>
		</form>
	</section>

	<h1>Pas encore inscrit ?</h1>
	<p>
    <a href="{{ path('app_register') }}" class="btn-add">Inscription</a>
    </p>
{% endblock %}
