{% extends 'base.html.twig' %}

{% block title %}Votre recette{% endblock %}

{% block body %}
        <h1 class="form-title">Ajoutez une nouvelle recette</h1>

        {{ include('recipe/_form.html.twig') }}

        <div class="form-footer">
            <a href="{{ path('app_recipe_index') }}" class="btn-back">
                <i class="ion-arrow-left-c"></i> 
                Revenir à toutes les recettes
            </a>
        </div>
{% endblock %}
